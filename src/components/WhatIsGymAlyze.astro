<section class="section">
  <div class="container">
    <h2 class="section-title">Your Gym, <span class="gradient-text">Upgraded by AI</span></h2>
    <div class="content-grid">
      <div class="content-text">
        <p class="large-text">
          GymAlyze is an AI-powered fitness tracking system that transforms ordinary gyms into intelligent, data-driven spaces — without installing any new hardware.
        </p>
        <p>
          It uses your existing CCTV cameras to track member workouts in real time and deliver actionable insights to both members and gym owners.
        </p>
        <div class="feature-highlights">
          <div class="highlight">
            <span class="highlight-icon">🎯</span>
            <span>Real-time tracking</span>
          </div>
          <div class="highlight">
            <span class="highlight-icon">🔧</span>
            <span>No new hardware</span>
          </div>
          <div class="highlight">
            <span class="highlight-icon">📊</span>
            <span>Actionable insights</span>
          </div>
        </div>
      </div>
      <div class="content-visual">
        <div class="visual-container">
          <div class="ai-brain">
            <div class="brain-core"></div>
            <div class="brain-waves">
              <div class="wave"></div>
              <div class="wave"></div>
              <div class="wave"></div>
            </div>
          </div>
          <div class="data-points">
            <div class="data-point" style="top: 20%; left: 15%;">
              <span>Rep Count</span>
            </div>
            <div class="data-point" style="top: 60%; right: 20%;">
              <span>Form Score</span>
            </div>
            <div class="data-point" style="bottom: 30%; left: 25%;">
              <span>Weight</span>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .content-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }

  .large-text {
    font-size: 1.25rem;
    font-weight: 500;
    color: #1a1a1a;
    margin-bottom: 1.5rem;
  }

  .feature-highlights {
    display: flex;
    gap: 2rem;
    margin-top: 2rem;
  }

  .highlight {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    font-weight: 500;
  }

  .highlight-icon {
    font-size: 1.2rem;
  }

  .visual-container {
    position: relative;
    height: 400px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .ai-brain {
    position: relative;
    width: 200px;
    height: 200px;
  }

  .brain-core {
    width: 100%;
    height: 100%;
    border-radius: 50%;
    background: linear-gradient(135deg, #0a2b48 0%, #1b3b58 100%);
    display: flex;
    align-items: center;
    justify-content: center;
    position: relative;
    overflow: hidden;
  }

  .brain-core::before {
    content: '🧠';
    font-size: 4rem;
    animation: pulse 2s ease-in-out infinite;
  }

  .brain-waves {
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
  }

  .wave {
    position: absolute;
    border: 2px solid rgba(10, 43, 72, 0.3);
    border-radius: 50%;
    animation: ripple 3s linear infinite;
  }

  .wave:nth-child(1) {
    width: 250px;
    height: 250px;
    margin: -125px 0 0 -125px;
    animation-delay: 0s;
  }

  .wave:nth-child(2) {
    width: 300px;
    height: 300px;
    margin: -150px 0 0 -150px;
    animation-delay: 1s;
  }

  .wave:nth-child(3) {
    width: 350px;
    height: 350px;
    margin: -175px 0 0 -175px;
    animation-delay: 2s;
  }

  .data-point {
    position: absolute;
    background: white;
    padding: 0.5rem 1rem;
    border-radius: 20px;
    box-shadow: 0 4px 15px rgba(0, 0, 0, 0.1);
    font-size: 0.9rem;
    font-weight: 600;
    color: #0a2b48;
    animation: float 3s ease-in-out infinite;
  }

  .data-point:nth-child(2) {
    animation-delay: 1s;
  }

  .data-point:nth-child(3) {
    animation-delay: 2s;
  }

  @keyframes pulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
  }

  @keyframes ripple {
    0% {
      opacity: 1;
      transform: scale(0);
    }
    100% {
      opacity: 0;
      transform: scale(1);
    }
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-10px); }
  }

  @media (max-width: 768px) {
    .content-grid {
      grid-template-columns: 1fr;
      gap: 2rem;
    }

    .feature-highlights {
      flex-direction: column;
      gap: 1rem;
    }

    .visual-container {
      height: 300px;
    }

    .ai-brain {
      width: 150px;
      height: 150px;
    }

    .brain-core::before {
      font-size: 3rem;
    }

    .wave:nth-child(1) {
      width: 200px;
      height: 200px;
      margin: -100px 0 0 -100px;
    }

    .wave:nth-child(2) {
      width: 240px;
      height: 240px;
      margin: -120px 0 0 -120px;
    }

    .wave:nth-child(3) {
      width: 280px;
      height: 280px;
      margin: -140px 0 0 -140px;
    }
  }
</style>