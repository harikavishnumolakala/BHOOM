<section id="what-gymalyze-tracks" class="section" style="background: #f0fdf4;">
  <div class="container">
    <h2 class="section-title">What GymAlyze Tracks <span class="gradient-text">Automatically</span></h2>
    <p class="section-subtitle">No devices. No manual input. Just train — we track everything.</p>
    
    <div class="features-grid">
      <div class="feature-card">
        <div class="feature-icon">✅</div>
        <h3>Reps, Sets, and Rest Time</h3>
        <p>Automatically count every repetition and track rest periods between sets.</p>
      </div>
      
      <div class="feature-card">
        <div class="feature-icon">⚖️</div>
        <h3>Weight Lifted</h3>
        <p>Detect weight via machine recognition and positioning analysis.</p>
      </div>
      
      <div class="feature-card">
        <div class="feature-icon">🎯</div>
        <h3>Exercise Type</h3>
        <p>Auto-detect exercise types using advanced computer vision.</p>
      </div>
      
      <div class="feature-card">
        <div class="feature-icon">📐</div>
        <h3>Real-Time Form & Posture Score</h3>
        <p>Get instant feedback on your form and posture during workouts.</p>
      </div>
      
      <div class="feature-card">
        <div class="feature-icon">⚡</div>
        <h3>Fatigue Detection</h3>
        <p>AI-powered burnout risk alerts to prevent overtraining.</p>
      </div>
      
      <div class="feature-card">
        <div class="feature-icon">📈</div>
        <h3>Workout Progress</h3>
        <p>Track your progress over time with historical comparisons.</p>
      </div>
    </div>
  </div>
</section>

<style>
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
    margin-top: 3rem;
  }

  .feature-card {
    background: white;
    padding: 2rem;
    border-radius: 16px;
    box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    transition: all 0.3s ease;
    border: 1px solid #e2e8f0;
  }

  .feature-card:hover {
    transform: translateY(-5px);
    box-shadow: 0 10px 30px rgba(10, 43, 72, 0.15);
  }

  .feature-icon {
    font-size: 2rem;
    margin-bottom: 1rem;
    display: block;
  }

  .feature-card h3 {
    font-size: 1.25rem;
    font-weight: 600;
    margin-bottom: 0.75rem;
    color: #1a1a1a;
  }

  .feature-card p {
    color: #666;
    line-height: 1.6;
  }

  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr;
      gap: 1.5rem;
    }

    .feature-card {
      padding: 1.5rem;
    }
  }
</style>